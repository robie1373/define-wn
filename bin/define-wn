#!/usr/bin/env ruby

require './lib/define-wn'
module Define

  word = ARGV[0] || "carrot"

  defi = Word_lookup.new(word)
  puts "The definition is: #{defi.definition}"
  puts "Word type: #{defi.type}"
end
